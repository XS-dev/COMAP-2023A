clear all;
clc;
% 设置模拟参数
A1 = 2; % 初始振幅（较大的稳定态）
A2 = 1; % 最终振幅（较低的稳定态）
f1=1/107;
f = 2; % 震荡频率
d = 0.1; % 震荡衰减速率

t1= 0:1:214;
t2 = 215:1:430; % 时间向量
t3 = 431:1:860;

% 计算从一个较大的稳定态转变为一个较低的稳定态的函数
y1= 100.*sin(2 * pi * f1 * t1)+200;
y2 =100.*sin(2 * pi * 2/107 * t2)+200;
y3= 100.*sin(2 * pi * f1 * t3)+200;
k=[y1,y2,y3];
t=0:1:860;
yyaxis right;

plot(t/10,k/10);
ylabel('R(mm)')